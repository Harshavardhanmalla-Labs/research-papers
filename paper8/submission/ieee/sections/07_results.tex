\section{Results}
\label{sec:results}

All claims trace to
\texttt{paper8/results/primary\_v1/multi\_history\_results.csv}
(2{,}250 rows) and the derived
\texttt{hypothesis\_summary.json} and \texttt{cell\_window\_means.csv}.
Table~\ref{tab:main} reports the full per-(capacity, window) means for
all five strategies; Fig.~\ref{fig:trajectories} plots the
corresponding trajectories, and Fig.~\ref{fig:rho} summarises the
cell-mean recovery ratios.

\input{tables/tab_main}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig1_strategy_trajectories.pdf}
\caption{Mean P@50 trajectories for all five calibration strategies at
$K \in \{50, 100, 200\}$, $\lambda = 3$. EWMA-3 (orange) and trail3 (purple)
fall below lag-1 (green) at every $K$.}
\label{fig:trajectories}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig2_recovery_ratio.pdf}
\caption{Cell-mean recovery ratio $\bar\rho$ across $w \geq 2$ per
strategy. Dashed line at $\bar\rho = 0.5$ marks the pre-registered
H4 threshold; EWMA-3 sits well below it at every $K$.}
\label{fig:rho}
\end{figure}

\subsection{Cell-Mean Recovery Ratios}

The summary statistic across $w \geq 2$ per cell
(plotted in Fig.~\ref{fig:rho}):

\begin{center}
\small
\begin{tabular}{ccc}
\toprule
$K$ & $\bar\rho^{\textsf{lag1}}$ & $\bar\rho^{\textsf{ewma3}}$ \\
\midrule
50  & $+1.04$ & $+0.53$ \\
100 & $+0.99$ & $-1.37$ \\
200 & $-0.66$ & $-0.94$ \\
\bottomrule
\end{tabular}
\end{center}

EWMA-3 underperforms \textsf{lag1} at every cell. At $K = 50$ the
gap is $-0.51$ in recovery-ratio units; at $K = 100$ the gap is
$-2.36$, the largest single-cell delta in the table; at $K = 200$ the
gap is $-0.28$. The naive prediction ``smoothing helps at high
capacity, hurts mildly at low capacity'' is contradicted in both directions.

\subsection{Per-Window Detail}

\textbf{$K = 50$.} EWMA-3 matches \textsf{lag1} at W2 and W3 but
falls below it at W4 by $7.8$~pp and at W6 by $2.2$~pp, matching it
at W5 (Table~\ref{tab:main}). The mechanism is that
the W4 calibration target for EWMA-3 includes W1, W2, and W3 of the
calibration seeds, whose distributions have shifted by W4. The
\textsf{lag1} W4 calibration target uses only the W3 calibration
state, which is closer to the W4 evaluation state.

\textbf{$K = 100$.} The gap widens. At W3 \textsf{lag1} adds
$+3.0$~pp over \textsf{fixed}; EWMA-3 \textit{loses} $-5.6$~pp.
By W6, \textsf{lag1} gains $+4.0$~pp while EWMA-3 gains only
$+1.4$~pp. The H3 failure cell ($K = 100, w = 5$) is also where
\textsf{ewma3} and \textsf{trail3} diverge by $+10.6$~pp ---
the only window in the entire sweep where the smoothing-strategy
choice is first-order.

\textbf{$K = 200$.} Both \textsf{lag1} and EWMA-3 are hazardous: at
W2 and W3 the two are \textit{identical} (both lose $-5.9$ and
$-7.8$~pp against \textsf{fixed}, since at these early windows the
EWMA history reduces to the same selection as lag-1). The EWMA-3 gap
arises entirely from the dampened W4--W6 recovery: \textsf{lag1}
regains $+3.4$, $+1.0$, and $0.0$~pp over \textsf{fixed} at W4--W6,
whereas EWMA-3 regains only $+1.0$, $+0.1$, and $+0.1$~pp, so the
average over $w \geq 2$ tilts further negative for EWMA-3.

\subsection{Hypothesis Outcomes}

\input{tables/tab_hypotheses}

All four pre-registered hypotheses are rejected;
Table~\ref{tab:hypotheses} summarises the decision rules and outcomes.

\textbf{H1 (EWMA-3 $\geq$ fixed at $K = 200$).} Rejected. Two
window failures at $w \in \{2, 3\}$ with deltas of $-5.9$ and
$-7.8$~pp.

\textbf{H2 (EWMA-3 matches \textsf{lag1} at moderate capacity).}
Rejected. Five cell-window failures across $K \in \{50, 100\}$ with
$|\Delta| > 0.02$. The signs are negative (EWMA-3 worse than
\textsf{lag1}) in all five.

\textbf{H3 (EWMA-3 matches \textsf{trail3}).} Rejected at one
cell-window ($K = 100, w = 5$). The single failure is large
($+10.6$~pp) and reflects a specific grid-search disagreement at
that window; otherwise the smoothing-strategy choice between EWMA
and trail3 is consistently second-order.

\textbf{H4 (EWMA-3 recovery $\geq 0.5$ at $K = 200$).} Rejected.
Cell-mean recovery ratio is $-0.94$, negative rather than $\geq 0.5$.

\subsection{Smoothing-Penalty Mechanism}

The per-cell pattern is monotone in history length:
\textsf{lag1} $>$ \textsf{trail3} $\approx$ \textsf{ewma3} at every
$K$. The pre-registered EWMA $\eta = 0.6$ effectively averages
$\approx 2.5$ past windows; the equal-weight \textsf{trail3} averages
exactly 3. Both bring older calibration-seed states into the
fitting target, and at the simulated bi-weekly cadence those older
states are systematically less representative of the current window
than the most recent past window alone. The H3 single failure at
$K = 100, w = 5$ is the exception that confirms the rule: at that
window the grid-search optimum was on a flat region and the
$\eta = 0.6$ weighting moved EWMA's selection to a different local
optimum than trail3's equal weighting.

\subsection{Pre-Registration Deviations}

None. Strategy definitions, hypotheses, decision rules, and stop
rules were applied verbatim. The abstract was rewritten after H1
rejection in compliance with the protocol's stop rule before the
discussion was drafted.
