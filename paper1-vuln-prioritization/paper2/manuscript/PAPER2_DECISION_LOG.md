# Paper 2 — Decision Log

Working title: *Calibrated Vulnerability-Host Prioritization: Sensitivity and
Label-Robustness Analysis Using a Frozen Public-Sector-Shaped Benchmark.*

Prior topic ("NIST 800-53 evidence-quality benchmark") was **killed/pivoted** in
its Step 2.5 after close April-2026 OSCAL/compliance-evidence prior art (arXiv
2604.13767; IEEE 11398064 `[VERIFY]`). Not continued.

| Step | Date | Decision | Basis | Blocker / Next gate |
| --- | --- | --- | --- | --- |
| 1 — Research Validation | — | **CONDITIONAL GO** | Moderate-novelty companion to Paper 1; likely-neutral result; lower risk than killed compliance paper | Conditions: (1) pre-register, (2) meaningful public-feed calibration data, (3) robustness/sensitivity framing |
| 2 — Data Feasibility | — | **CONDITIONAL GO** (pending positive-count proof) | Feed clients + 31-product catalog + calibration/labels/stats all exist; snapshots empty; calibration N bounded by distinct labeled CVEs | **Blocker: distinct positive-CVE count** under CPE/catalog matching |
| 3 — Feasibility Probe (build) | — | CONDITIONAL GO | Built + tested `scripts/paper2_feasibility_probe.py`; sandbox run blocked (no network) | Gate: ≥50 positive distinct CVEs + non-degenerate calibration |
| 3.5 — Probe Execution | — | **CONDITIONAL GO** (gate UNRESOLVED) | Real feeds fetched: KEV 1,184; EPSS 3 days; NVD 120-day window 281 catalog-matched CVEs, EPSS coverage 100%, KEV-as-of-t0 1,159. Label A positives **0** — a universe-truncation artifact (full 487-day NVD fetch hit HTTP 429; needs API key) + single-30-day-horizon sparsity. Pipeline validated on real data; positive gate NOT validly measured. | **Two fixes required:** (1) `NVD_API_KEY` for full multi-year universe; (2) redesign to aggregate distinct positive CVEs across many monthly t0 windows (v3 era). Then re-apply gate. PIVOT fallback if still <20–50 after fixes. |
| 3.6 — Multi-t0 + Hardened Acquisition | 2026-05-27 | **CONDITIONAL GO** (gate UNRESOLVED; `NVD_API_KEY` required) | Both Step-3.5 fixes implemented + tested (762 pytest pass): (1) NVD acquisition hardened — ≤120-day chunking, per-chunk cache + `--resume`, `NVD_API_KEY` + pacing, HTTP-429 preserves partial chunks & writes a resume command; (2) `--multi-t0` mode aggregates **UNIQUE distinct positive CVEs** across monthly t0 windows (≠ pair count, ≠ per-window event sum) with CVE-level-deduped calibration smoke. Real **cached** 4-window demo (universe 2024-05-04..2024-08-31) validated mechanics end-to-end: 13,050 NVD recs → 281 catalog CVEs, unique positives **0** (same narrow-universe truncation artifact). Full-scale config (2021-09..2025-02, 11 chunks) **BLOCKED**: no `NVD_API_KEY` in sandbox. | **Single blocker: `NVD_API_KEY`.** Run `make paper2-multit0-probe` with the key, then re-apply gate on UNIQUE positive CVEs: ≥50 + non-degenerate → GO (Step 4); 20–49 → CONDITIONAL; <20 → PIVOT to robustness/sensitivity-only Paper 2. |
| 3.7 — Full multi-t0 run w/ key | 2026-05-28 | **CONDITIONAL GO** (gate UNRESOLVED; `NVD_API_KEY` still absent) | Task assumed a key was present; **verified absent 3 ways** (`os.environ`/`printenv`/shell expansion; no `.env`), so no key was faked. Ran `make paper2-multit0-probe` for real: paced unauthenticated fetch of universe 2021-09-01..2025-02-01 (11 chunks) hit **HTTP 429 on chunk 3** (`2022-04-29..2022-08-26`); **2/11 chunks fetched & cached** (7,634 + 8,375 real records) for `--resume`; exit 3, no fabricated counts. Per-window/aggregate/calibration files intentionally **not written** (acquisition blocks before the labelling pass). Positive-CVE count therefore **unmeasured** (NOT 0). Paper 1 freeze OK before/after. | **Same single blocker: a real `NVD_API_KEY` in the probe's process env.** Then `make paper2-multit0-probe` (add `--resume`, `--nvd-sleep-seconds 0.6`) and re-apply the gate. **Step 4 NOT allowed** until the gate passes. |
| 3.8 — Resumed multi-t0 w/ key | 2026-05-28 | **PIVOT** (gate RESOLVED; calibrated-prioritization Paper 2 infeasible) | Key supplied in-message (never echoed/logged; only `api_key_present: true` recorded). Real keyed resume completed cleanly: **11/11 NVD chunks** (2 reused from 3.7 cache + 9 fetched; **110,224 records**; 0 HTTP-429s with 0.6s pacing); 18 monthly t0 windows fully evaluated; **2,688 catalog-matched CVEs** (≈2.8% of CPE'd); EPSS coverage 17/18 windows ≥99.3% (1 graceful miss); leakage-safe `(t0, t0+H]` labels; no warnings. **Unique positive distinct CVEs = 7** (events: 2,2,1,1,0×11,1; each unique positive appears in exactly one window). 7 < 20 → fires literal PIVOT; Label-A event rate ≈0.014% also fires "too sparse" PIVOT. Calibration not attempted (7 < 50). Paper 1 freeze OK before/after. | **Step 4 NOT allowed** for the calibrated framing. Recommended Paper 2 pivot: **robustness/sensitivity-only** (real-feed features, fixed/published weights; A/B label, capacity, blackout, EPSS-coverage sweeps). Optional one-shot before pivoting: expand catalog 31→~100+ products and re-run (only with explicit user OK; likely ceiling tens, not hundreds). |
| 3.9 — Pivot decision + research validation | 2026-05-28 | **Pivot chosen: Option A** (robustness/sensitivity-only Paper 2). **CONDITIONAL GO** to Step 4 pending nine fixes (F1–F9) | New working direction: *"Robustness of Context-Aware Vulnerability Prioritization Under Sparse Exploit Labels — A Public-Feed Sensitivity Study Using a Frozen Synthetic Endpoint Benchmark."* Validation report grounded in Step-3.8 measured facts: refined Q1–Q4 (sensitivity, not superiority); five tightly-bounded contributions (C1–C5); 8 candidate axes (capacity / strategy / label / feature-ablation / blackout / approver / EPSS-era / catalog-strictness) with v3-only era + cpe_exact + no PoC defaults; metric→claim binding restricts primary claims to EHD/EEHDA/capacity/feasibility/rank-churn/sensitivity-deltas (sparse-positive ranking metrics labelled diagnostic-only); sparsity-honest stats (paired seeds, BCa CIs, pre-registered MDE, Wilcoxon+Holm only where powered); 10 risks + 6 kill criteria (K1–K6) enumerated; skeptical scores: publishability 5/10, novelty 4/10, EB1A value 4/10. Catalog expansion deferred. | Step 4 **NOT** allowed until F1–F9 land in `STEP4_PREREGISTRATION.md`: F1 prior-art `[VERIFY]`; F2 fixed-weight source locked (placeholder vs literature prior); F3 metric→claim binding; F4 MDE/power pre-reg; F5 kill criteria as STOP rules; F6 minimal factorial enumerated; F7 freeze invariant reasserted; F8 compute estimate decided; F9 venue plan + "change-our-minds" clause. |
| 3.10 — F1 prior-art verification | 2026-05-28 | **CONDITIONAL KEEP** (substantial framing pivot required); F1 complete | Targeted web/library search executed across 12 areas / 18 queries (Google Scholar / arXiv / ACM DL / Springer / FIRST / CISA / NIST / SSRN / vendor blogs as context). **15 academic/standard sources catalogued (table P1–P15)**; 6 HIGH-threat (Sherif 2026 KRI ablation arXiv 2603.12450; VULCON 2018 ACM TOPS doi 10.1145/3196884 capacity-constrained; Deep VULMAN 2023 ESwA DRL+IP; Roytman "Capacity is King" strategy×capacity sweep; Ravalico 2025 SSRN 5147459 EPSS temporal dynamics on 45k+; Jiang 2025 survey arXiv 2502.11070 82-study taxonomy); 4 MEDIUM-HIGH (Koscinski 2025 arXiv 2508.13644 4-system disagreement; VM-Chaining arXiv 2506.01220; SSVC v2; NIST CSWP 41 LEV [VERIFY]). **Falsification:** sensitivity/ablation/capacity headlines are LARGELY OCCUPIED; only the failure-aware multi-t0 calibration-gate + the specific KEV-only-catalog-restricted sparsity figure (7/2,688/18) survive as novel. Honest scoring updated: publishability 4/10, novelty 3/10, EB1A 3/10. | **Step 4 still NOT allowed.** Required framing changes (must land in STEP4_PREREGISTRATION.md): (1) drop "Robustness of Context-Aware Vulnerability Prioritization" title; (2) repurpose sensitivity sweeps from PRIMARY to CONFIRMATORY secondary; (3) elevate the negative feasibility finding to PRIMARY; (4) reframe multi-t0 gate as the methodology contribution; (5) acknowledge synthetic-fleet as limitation, NOT novelty; (6) require P1–P15 citations + visible differentiation in Related Work; (7) drop "fixed/published weights" wording (no widely-published prior for our 7-feature scheme). F2–F9 still owed. |
| 3.11 — F2 fixed-weight source locked | 2026-05-28 | **Option C — weight choice is a sensitivity axis.** F2 COMPLETE; no code written | Inspected `src/paper1/model/weights.py`: FEATURE_COLUMNS=[E,K,S,C,X,U,R]; scoring `=w_E E + w_K K + w_S S + w_C C + w_X X + w_U U − w_R R`; existing built-ins `w_uniform`, `w_hand={E:0.20,K:0.20,S:0.10,C:0.20,X:0.15,U:0.10,R:0.05}`, `w_logit_placeholder`/`w_lin_placeholder` mirror `w_hand`; no calibrated weights ever registered. Literature review against F1 sources: **no source publishes numeric 7-feature weights** — EPSS publishes a single probability, CVSS publishes severity sub-scores, KEV is binary, SSVC is a tree, Sherif's KRI is *learned*. Option B rejected (would require inventing numbers). **Option C locked** with six pre-registered design-prior vectors: `w_uniform` (1/7 each), `w_paper1_placeholder` (= existing `w_hand`), `w_epss_dominant` (E=0.50), `w_cvss_dominant` (S=0.50), `w_kev_dominant` (K=0.50), `w_context_dominant` (C=0.30, X=0.25). All sum to 1, all non-negative, all R-as-cost. Each vector tagged `weight_source="design_prior"` (never "calibrated"/"learned"/"literature-derived"). Seven kill/stop rules W1–W7 pre-registered (indistinguishability OK, EPSS-only dominance OK, context-dominant-worse OK, no re-weighting after results, no learned weights in Paper 2, no literature-numeric masquerading, `proposed_full` stays placeholder-labelled). | **Step 4 still NOT allowed.** F3–F9 owed + Step-3.10 framing changes. Step-4 implementation will register four new built-ins (`w_*_dominant`) without touching the existing four; ablation sweeps must run on the *family*, not only on `w_logit_placeholder`; every result row must carry `weight_family / weight_source / weight_vector_name / citation-chain`. |
| 3.12 — F3 metric → claim binding | 2026-05-28 | **F3 COMPLETE.** Pre-registered metric→claim binding locked; no code written | Inventoried `src/paper1/evaluation/` (no new metrics needed): EHD/EEHDA, relative_to_epss, fraction_of_oracle, precision/recall/nDCG@k, rank_churn, scheduler_feasibility_rate, capacity_efficiency, risk_acceptance_rate, kev_deadline_breach_rate/latency, hash_chain_validity, audit_record_count, imputation_rate, MDE/Wilcoxon/Holm/BCa infrastructure. **13 claims pre-registered (CLM-A1, CLM-A2, CLM-B1..3, CLM-C1..4, CLM-D1 diag, CLM-E1..2, CLM-F1..2, CLM-G1 diag)** each with exactly-one primary metric, secondary metrics, required axis, allowed + forbidden interpretations, minimum data condition, and stop rule. **Class A primary = unique positive distinct CVEs** (never pair count, never event sum). **Class B primary = EHD** (paired-seed ΔEHD vs `epss_only` with BCa CI; descriptive, not superiority). **Class C primary = sensitivity ΔEHD across A3 weight-family / A1 capacity / A6 blackout / A5 feature-ablation**. **Class D & G = diagnostic-only** (precision/recall/nDCG@k and KEV breach rate; gated by Step-3.8 sparsity). Axis → claim mapping locked (A1/A2/A3 in minimal factorial; A5/A6 confirmatory required; A4/A7 secondary; A8 v3 + A9 cpe_exact held fixed). **13 forbidden claims enumerated** (no superiority, no calibration improves, no production-readiness, no compliance, no pair-count-as-N, no cherry-pick, no vector-quality-ranking, no synthetic≈real, no EPSS v4, no PoC without gate, etc.). 13 stop rules (S-A through S-G1) collected. | **Step 4 still NOT allowed.** F4 (MDE/power), F5 (kill criteria as STOP rules), F6 (minimal factorial cells), F7 (freeze invariant restated), F8 (compute estimate), F9 (venue plan + change-our-minds) still owed; Step-3.10 framing changes still owed. Step-4 impl must per-row-tag every result with `claim_id / primary_metric_name / axis_cell_id / weight_* / freeze_status_before / freeze_status_after / audit_hash_chain_valid`; CI must scan manuscript for forbidden phrases (Paper-1 Phase 21/22A precedent). |
| 3.13 — F4 MDE / power pre-registered | 2026-05-28 | **F4 COMPLETE.** Inference policy locked; no code written; Paper 1 freeze OK before/after read-only variance-prior extract | Extracted real variance prior from `results/primary_full_v1/metrics/per_seed_metrics.csv` (30 seeds × 13 strategies): **baseline `epss_only` EHD = 1,121,903 host-days**; paired-Δ std vs `epss_only` 2,331–3,673 hd; `kev_first − epss_only` identically 0 (degenerate). **Meaningful threshold locked = 5,000 hd (≈0.45% baseline)** — task's suggested 1,000 hd / 0.10% was below the per-seed noise floor (would collapse the gate); operational threshold of 1,000 hd kept for *reporting* only. **MDE_d at n=30, α=0.05, power=0.80, two-sided = 0.5292**; conversion at ×1.0/×1.5/×2.0 sparsity inflation puts MDE-hd in 1,234–3,888 hd range — **below 5,000 hd at all factors**. **Allowed for inference**: CLM-B1 (per pair, except `kev_first`), CLM-B2 (inherits), CLM-C1, CLM-C2, CLM-C3, CLM-C4. **Dropped (degenerate)**: CLM-B1 `kev_first − epss_only`. **Descriptive-only**: CLM-B3 (oracle ≈ random / oracle − epss_only mean = −2,576 hd < meaningful 5,000 hd; oracle weak under sparse positives — SM-3 disables oracle inference). **Diagnostic-only (F3 binding inherited)**: CLM-D1, CLM-G1. **Sentinel / binary / measurement**: CLM-A1, CLM-A2, CLM-E1, CLM-E2, CLM-F1, CLM-F2. Test policy: Wilcoxon + Holm only when n≥30, std(Δ)>0, MDE-hd ≤ 5,000 at chosen inflation, pair in pre-registered Holm family (B1=5 pairs; C1=15 or smaller; C2=3; C3≤3; C4=5/vector), no leakage warning, audit chain OK. Six stop rules SM-1..SM-6 (degeneracy auto-drop; prospective inflation escalation; oracle inference disabled; effect-size honesty if Holm passes but BCa CI overlaps 0 or ⊂ [−5k,+5k]; no implicit upgrade of diagnostics; K6 freeze invariant). | **Step 4 still NOT allowed.** F5 (kill criteria K1–K6 as runtime STOP rules), F6 (enumerate the specific factorial cells; constrained by F8), F7 (re-assert freeze invariant in STEP4_PREREGISTRATION.md), F8 (decide compute budget; 30 seeds × 13 strategies × 4 capacities × 6 vectors × {1 blackout × 1 approver} central + secondary ablation/blackout tables = several thousand runs, hours-to-days on a laptop), F9 (venue plan + change-our-minds) still owed; Step-3.10 framing changes still owed. Step-4 impl must per-row-tag `n_pairs / paired_std_dhd / sparsity_inflation_factor / mde_hd / meaningful_threshold_hd / inference_status / holm_family_id / holm_family_size` and gate `wilcoxon_signed_rank`/`compare_to_baseline` behind a policy shim. |
| 3.14 — F5 kill criteria → runtime STOP rules | 2026-05-28 | **F5 COMPLETE.** Canonical stop-rule registry locked; YAML schema sketched; no code written | Encoded **canonical Step-3.14 K1–K6** (calibration infeasibility / robustness-null / public-feed sparsity / scheduler infeasibility / audit-freeze failure / Paper-1 freeze modified) with computable triggers + locked thresholds: K1 unique_positive < 20 (already TRIGGERED, value=7); K2 |median ΔEHD| < 1000 hd AND BCa CI ⊂ ±5000 hd across **every** primary axis (post-run; reframe-as-negative if all axes fire, scope-reduce if some); K3 per-window positives < 3 in >75% windows OR unique < 20 (already TRIGGERED via OR branch); K4 scheduler_feasibility_rate < 0.95 (per-cell exclude with footnote); K5 hash-chain invalid OR freeze fail OR **leakage warning fires** (K5.a sub-trigger from Step-3.9 K5) → hard halt; K6 `verify-primary-freeze` non-zero pre/post → hard halt entire step. Step-3.9 K2 (variance dominated by seed noise CV_within > CV_across) promoted to **K8**; Step-3.9 K3 (catalog-perturbation drift > 30%) promoted to **K7**; Step-3.9 K4 (venue insufficiency) deferred to F9 (no runtime threshold for venue fit). **Merged registry** organized into 7 categories (acquisition / calibration / metric-validity / statistical-inference / scheduler / audit-freeze / manuscript-claim) × 4 stages (pre-reg / pre-run / per-cell / post-run / manuscript-gen) × 6 enforcement levels (hard-halt / exclude-cell / downgrade-claim / diagnostic-only / report-only / pivot). YAML schema with `constants`, per-rule `rule_id / severity / stage / category / metric / artifact / trigger / action / forbid / affects / audit_record_template / status_now` ready to be materialized at Step 4 startup. **Currently triggered: K1, K3, S-A (mirror of K1).** Step-4 implementation contract: load registry → pre-flight gate → per-cell gate → policy-shimmed inference → post-run gate → manuscript-CI gate → audit-completeness self-policing; module `paper2_runtime.stop_rules` to be created in Step 4 (NOT under `src/paper1/`). | **Step 4 still NOT allowed.** F6 (enumerate the specific minimal factorial cells under F8 budget), F7 (re-assert freeze invariant inside `STEP4_PREREGISTRATION.md`), F8 (compute estimate), F9 (venue plan + "change our minds" clause) still owed; Step-3.10 framing changes still owed. No `[CITATION]` placeholders in the eventual paper; `[VERIFY]` items from F1/F2 (VULCON DOI / CSWP 41 LEV / VulnScore) re-verify before manuscript stage. |
| 3.15 — F6 minimal factorial cells enumerated | 2026-05-28 | **F6 COMPLETE.** Cells locked in CSV + YAML; no code; no experiments | Locked central defaults: n_seeds=30, label_a_kev_only, EPSS v3, cpe_exact_existing31, blackout=primary, approver=A, capacity=0.01, 18-month t0 set. **Primary table (12 cells)**: 6 baselines (random/cvss_only/epss_only/kev_first/cvss_x_epss/cvss_plus_epss_plus_kev) + 6 proposed_fixed_prior cells (one per F2 weight vector) — supports CLM-B1/B2/B3 and CLM-C1 from primary outputs alone (no extra runs for the C(6,2) Holm family). **Capacity sensitivity** = 5 strategies × {0.005,0.01,0.02,0.05} → 15 new + 5 reused (CLM-C2). **Blackout sensitivity** = 3 strategies × {none,light,primary,strict} → 9 new + 3 reused (CLM-C3). **Feature ablation** = 2 vectors (w_paper1_placeholder, w_context_dominant) × {full + 6 remove_*} → 12 new + 2 reused (CLM-C4). **Total new runnable = 48; reused = 10; deferred = 8** (approver-B, EPSS v4, catalog expansion, PoC Label B, fuzzy CPE, n=50/100, all-15-pairwise weight comparisons [no extra runs needed], GBT/learned [FORBIDDEN by K1]). **Total planned seed-runs at n=30: 1,440.** Fallback options F8-a..F8-f pre-listed (seed reduction / capacity / blackout / ablation collapses / pilot+primary) but selection deferred to F8. Runtime estimate range **8–24 h on a laptop** assuming shared per-seed computation across the 48 cells; pessimistic multi-day without shared computation. K1 / K3 trigger forbids any calibrated / learned / GBT cell. Files: `paper2/design/STEP3_15_MINIMAL_FACTORIAL_CELLS.{csv,yaml}` + `paper2/manuscript/STEP3_15_FIX_F6_MINIMAL_FACTORIAL.md`. | **Step 4 still NOT allowed.** F7 (re-assert freeze invariant inside `STEP4_PREREGISTRATION.md`), F8 (compute estimate + fallback selection from F6 §8), F9 (venue plan + change-our-minds) still owed; Step-3.10 framing changes still owed. Step-4 impl must load the YAML verbatim; refuse cells not in it; per-row tag `cell_id / table_group / claim_id / inference_family_id / weight_vector / capacity_ratio / blackout_policy / approver_policy / label_source / epss_era / catalog_strictness / ablation / reuse_of_cell_id / run_status`; reuse-only consumers cannot bypass K6 freeze invariant. |
| 3.16 — F7 Paper-1 freeze-invariant contract | 2026-05-28 | **F7 COMPLETE.** Contract + wrap pattern + audit-record schemas + weight-registry lock test locked; no code written | Reference manifest verified: `results/primary_full_v1/FREEZE_MANIFEST.json` exists, 201,491 B, SHA-256 **`750e144b…b022833`**, `verify-primary-freeze` OK (390 audit logs valid). K6 / SM-6 / S-F2 restated as a binding contract: every Paper-2 batch wraps with **pre-flight** (`make verify-primary-freeze PYTHON=.venv/bin/python` + manifest SHA + `git status --porcelain`) → run → **post-run** (same three) → 5 hard assertions (before OK; after OK; SHA equal; no Paper-1 paths modified; `freeze_primary_invocation_count==0`); any failure = K6 HARD HALT. **Allowed writes**: `paper2/`, `results/paper2/`, `data/cache/`, `data/snapshots/`. **Forbidden writes**: `results/primary_full_v1/`, `paper/{tables,figures,manuscript,acm,eb1a}/`, `FREEZE_MANIFEST.json`. **Forbidden ops**: `make freeze-primary`, `make inspect-primary --freeze`, `paper1.experiments.inspect.freeze(...)`, `register_calibrated_weights(...)` (K1). **Per-batch witness**: one freeze witness per batch, propagated as `freeze_witness_id` on every cell row; reused references inherit; long batches optional periodic check every N=8 cells; recommended at-most-one batch per F6 table group. **4 audit files** per batch (`freeze_witness_before.json`, `freeze_witness_after.json`, `freeze_invariant_result.json`, `freeze_invariant_result.md`) + append-only `paper2/audit/batches/index.json`. **CI gate** rejects manuscript table regen if any of 8 conditions fail (missing witnesses; SHA mismatch; non-zero exit; Paper-1 path in git status; `freeze_primary` invoked; row missing `freeze_witness_id`; row's batch not OK). **Paper-1 weight-registry lock test** specified: assert exact keys + exact values of `w_uniform`/`w_hand`/`w_logit_placeholder`/`w_lin_placeholder` + sum-to-1 + R≥0 + reject overwrites + verify F2 vector registrations leave built-ins unchanged. Step-4 implementation lives under new `paper2_runtime/` package (NOT `src/paper1/`). | **Step 4 still NOT allowed.** F8 (compute budget + fallback selection from F6 §8) and F9 (venue plan + change-our-minds) still owed; Step-3.10 framing changes still owed. The F7 weight-registry lock test must land in `tests/test_paper1_weights_locked.py` during Step-4 implementation and pass on every push touching `src/`, `paper2/`, or `tests/`. |
| 3.17 — F8 compute budget + fallback locked | 2026-05-28 | **F8 COMPLETE.** Chosen plan = **F8-f pilot-then-primary**; planned-batches YAML written (does NOT mutate F6); no code | Envelope locked: **target ≤12 h, hard max 18 h**; abort at ×1.5 estimate; ≤16 GB RAM; ≤25 GB disk; **seed-level parallelism, max_workers = 4** (2 if mem<16 GB); per-cell + per-t0 + acquisition + per-batch-summary checkpoints; 2 overnight runs max. **Pilot: 4 batches, 48 cells × 6 seeds = 288 seed-runs, est. 2–4 h.** **Primary: 4 batches, 48 cells × 30 seeds = 1,440 seed-runs, est. 9.8–19.5 h serial / 3–6 h with 4 workers** (only if pilot gate `PROCEED_TO_PRIMARY_NO_FALLBACK`). Pilot gate computes `measured_per_seed_run_seconds` → `projected_primary_runtime_hours`; if > 18 h, fires deterministic **fallback order F8-e → F8-d → F8-c → F8-b → F8-a** (last resort). `pilot_only_fallback.enabled = false` (closed by default). **Shared-computation architecture (binding)**: NVD universe ONCE/run, EPSS/KEV as-of maps ONCE/t0, fleet ONCE/seed, pair frames ONCE/(seed,t0), feature frames ONCE/(seed,t0); reuse across strategies/weight-vectors/capacities/blackouts/approvers/ablations within (seed,t0); scheduler + metrics per cell. F7 freeze wrap on EVERY batch; per-batch stop-rule gate registry (pre/per_cell/per_comp/post/manuscript). Abort/resume: ×1.5 budget overrun → pause+manual approval; K6/K5 fail → hard halt; K2 all-axis fire → reframe-as-negative; K4 fire → exclude cell with footnote; resume skips completed checkpoints unless `--force`. Files: `paper2/design/STEP3_17_PLANNED_BATCHES.yaml` (companion to F6; not mutating it) + `paper2/manuscript/STEP3_17_FIX_F8_COMPUTE_BUDGET.md`. | **Step 4 still NOT allowed.** F9 (venue plan + change-our-minds clause) still owed; Step-3.10 framing changes still owed. Step-4 impl: load F6 + F8 YAMLs at startup; refuse batches whose `cell_filter` resolves to cells absent from F6; seed-level parallel runner; per-cell `_meta.json` carries `freeze_witness_id`; pilot-gate evaluator emits `paper2/audit/pilot_gate_decision.json` before primary may launch; fallback re-planner emits a NEW `STEP3_17.x_PLANNED_BATCHES_FALLBACK_<id>.yaml` (F6 unchanged); resume never recomputes unless `--force`. |
| 3.18 — F9 venue plan + change-our-minds | 2026-05-28 | **F9 COMPLETE. F1–F9 all complete.** Step 4 pre-registration **ALLOWED** (document only; no experiments) | **Framing locked** (Step-3.10 §9 items 1–7 ratified): drop "Robustness…" headline; elevate failure-aware gate + negative result to PRIMARY contribution; demote sensitivity sweeps to CONFIRMATORY; acknowledge synthetic fleet as Limitations (not novelty); P1–P15 cited with visible differentiation; drop "fixed/published weights" wording; no `[CITATION]` placeholders in final manuscript. **Title #1 recommended**: *"When Calibration Fails: A Failure-Aware Public-Feed Gate for Vulnerability Prioritization Under Sparse Exploit Labels"*; #2/#3 held as alternates. **Venue strategy**: primary = **USENIX CSET** (reproducibility/methodology workshop, strong fit); backup 1 = **LASER** (NDSS/ACSAC-colocated, explicit null-results friendly); backup 2 = **ACM DTRAP** (journal, methodology/measurement; Jacobs et al. EPSS paper landed here). Top-tier (USENIX Sec / S&P / CCS / NDSS) rejected as unlikely fit per Step-3.10 honest assessment. **Change-our-minds clause (computable triggers)**: KILL A.1 (no venue home after 3 attempts), A.2 (pilot projection > F8 hard max even after full F8-a fallback), A.3 (K2 fires every axis + reviewer rejects gate as trivial), A.4 (everything degenerate), A.5 (no insight beyond sparsity number), A.6 (later-discovered prior art duplicates gate); PIVOT B.1 (all Δs zero → pure methodology+sparsity paper), B.2 (catalog expansion needed but deferred), B.3 (EPSS-only dominates → "EPSS hard to beat" headline), B.4 (only acquisition+gate publishable → artefact paper); KEEP C.1–C.5 (gate stays novel; pilot feasible; ≥1 axis interpretable; reproducible artefact; venue fit plausible). **Step-4 entry criteria**: 11 items locked, including K1+K3-triggered status with measured `7` in pre-registration abstract, no learned-claims, F8-f plan adopted verbatim. **Carried `[VERIFY]`**: VULCON DOI 10.1145/3196884 (ACM DL still 403; re-verify at submission), NIST CSWP 41 LEV (URL real, PDF unparseable; human re-read at submission), VulnScore Springer IJIS 2025 (re-verify if retained), CSET-2026/LASER-2026 CFPs (`[VERIFY at submission time]`). **NO citation fabricated.** | **Step 4 pre-registration ALLOWED** as a DOCUMENT-ONLY deliverable: `paper2/manuscript/STEP4_PREREGISTRATION.md` containing title + abstract + framing + venue + change-our-minds + references to F2–F8 (does NOT duplicate them) + carried `[VERIFY]` list + handoff to Step 5 (implementation). **No experiments. No code. No Paper-1 modification.** Step 5 (runner implementation) and Step 6 (pilot launch) remain blocked until Step-4 pre-registration is itself complete. |
| 4 — Pre-registration document authored | 2026-05-28 | **Step 4 COMPLETE. Pre-registration locked.** Design is now immutable until a Step-4.x supplement says otherwise | One new artefact produced: `paper2/manuscript/STEP4_PREREGISTRATION.md` (20 sections). **§1** document status: no experiments, no code, no Paper-1 modification. **§2** title locked at #1 *"When Calibration Fails: A Failure-Aware Public-Feed Gate for Vulnerability Prioritization Under Sparse Exploit Labels"*; #2/#3 held. **§3** abstract surfaces the K1/K3-triggered measurement (7 unique positive CVEs / 2,688 catalog CVEs / 18 monthly t0 windows / calibration not attempted). **§4** primary research question + 5 sub-questions Q1..Q5 mapped to CLM-A1/A2/B1..B3/C1..C4/E1/E2/F1/F2; CLM-D1/G1 explicitly NOT granted research-question status. **§5** prior-art position (P1–P15 from F1) + surviving novelty sentence verbatim. **§6** seven Step-3.10 framing changes each ticked off with one-line how-we-honor-it statement. **§§7–14** reference F2/F3/F4/F5/F6/F7/F8/F9 with summary tables but no duplicated normative content. **§15** planned outputs schema for Steps 5–9 (runner package; tests; audit logs; pilot configs; freeze witnesses; pilot gate decision; per-cell metric frames; aggregate tables only after Step 8; manuscript only after Step 9). **§16** 15 forbidden claims enumerated (no superiority / no calibration / no production / no compliance / no learned / no pair-count-as-N / no post-hoc selection / no vector-quality-ranking / no synthetic≈real / no EPSS v4 / no PoC w/o gate / no significance language near diagnostic-only metrics). **§17** 4 carried `[VERIFY]` items with re-verification triggers (no fabrication). **§18** Step 5 handoff: 8 constraints (new `paper2_runtime/` package not under `src/paper1/`; no design changes without supplement; freeze-verify first; primary blocked until pilot passes; K1 forbids calibrated cells; no manuscript text; no `[CITATION]` placeholders; freeze-wrap even on tests). **§19** sources-of-truth registry (14 files; immutable). **§20** invariants honored. | **Step 5 ALLOWED** as an **implementation-only** step: build `paper2_runtime/` package + tests + `paper2/audit/stop_rules.log` per F5/F7 contracts; NO experiment execution; NO design changes; primary remains blocked until Step 6 pilot completes and Step 7 pilot-gate decision = `PROCEED_TO_PRIMARY_NO_FALLBACK`. Steps 6 (pilot) and 7 (pilot evaluation) remain blocked behind Step 5 completion. |
| 5 — Runtime contract loaders + freeze wrapper + stop-rule registry + weight locks | 2026-05-28 | **Step 5 COMPLETE.** Runtime package built; all tests pass; freeze OK before/after; NO experiments run | New package **`paper2_runtime/`** (sibling to `src/paper1/`, not under it): `__init__.py`, `stop_rules.py` (F5 YAML registry loader + static evaluator + 5-artefact writer), `freeze_invariant.py` (F7 pre-flight/post-run wrap with 5 hard assertions + 4 witness files; refuses Paper-1 paths; rejects manifest SHA mismatch; context manager raises `Paper1FreezeInvariantViolation` on any failure), `cell_loader.py` (F6 CSV loader + planned/reused/deferred filters + dup check + table-group grouping), `batch_loader.py` (F8 YAML loader + validation + cross-check vs F6), `weights.py` (registers four F2 vectors via `register_weights`; snapshot-asserts Paper-1 built-ins untouched; raises if name collision with `w_uniform`/`w_hand`/`w_logit_placeholder`/`w_lin_placeholder`), `inference_policy.py` (SM-1 degeneracy auto-drop, SM-3 oracle disabled, SM-5 diagnostic-significance rejection, Holm-family gate, `kev_first` and `oracle` pair drops), `run_planner.py` (read-only F6+F8 summariser; `PROCEED_TO_PRIMARY_NO_FALLBACK` gate). YAML registry materialised at `paper2_runtime/STEP3_14_STOP_RULES_REGISTRY.yaml` with **28 rules** (K1–K8 + S-A..S-G1 + SM-1..SM-6 + K5.a/S-F1/S-F2/SM-6 aliases) and closed-set enforcement/severity values. Tests added (7 files; **53 new tests** including K1/K3/K6/K5.a triggers, hash-mismatch detection, context-manager failure path, no-freeze-primary-invocation scan, Paper-1 built-in immutability post Paper-2 registration). One pyproject change: `pythonpath = ["."]` added to pytest config (makes `paper2_runtime` importable; not a design change). One bug-fix to `paper2/design/STEP3_17_PLANNED_BATCHES.yaml` (quoted `~50` etc. to prevent YAML null-parse; outdented `pilot_only_fallback`; structural only, no compute-plan number changes). Makefile: `paper2-runtime-test`, `paper2-verify-freeze`, `paper2-plan` targets added. **Checks**: full pytest `815 passed`; ruff clean; compileall OK; `make verify-primary-freeze` OK before AND after; `make paper2-plan` prints F8-f summary with 48 runnable cells / 288 pilot seed-runs / 1,440 primary seed-runs / fallback order F8-e..F8-a. **Paper 1 frozen artefact UNTOUCHED.** | **Step 6 (pilot launch) ALLOWED** as a **document-and-runner-config-first** step. Step 6 must: (a) build the per-batch run config files that the runner consumes; (b) implement the per-cell scorer/scheduler/metric writer for one pilot batch end-to-end; (c) wire the F7 freeze wrap; (d) actually launch ONE pilot batch (`B-pilot-primary`, 12 cells × 6 seeds = 72 seed-runs) as a smoke; (e) NOT launch the other 3 pilot batches until Step 7 evaluates Step 6 against the F5 stop-rule registry. Step 7 (pilot evaluation + full pilot) and Step 8 (primary launch, only if `PROCEED_TO_PRIMARY_NO_FALLBACK`) remain blocked behind Step 6 completion. |
| 6 — One-batch pilot runner + B-pilot-primary smoke | 2026-05-28 | **Step 6 COMPLETE.** Runner built; all tests pass; **B-pilot-primary smoke completed on real cached public-feed data**; freeze OK before/after; Paper 1 UNTOUCHED | New module **`paper2_runtime/batch_runner.py`** (~ 380 lines): `STEP6_ALLOWED_BATCH="B-pilot-primary"`, `load_batch`, `preflight_stop_rules` (calls F5 `evaluate_known_static_rules` with Step-3.8 context; writes 5 evaluation artefacts; refuses any calibration / learned cell), `run_cell_seed` (per-cell scoring via Paper-1's linear formula + F2 vectors; simple top-K scheduler; simplified EHD; `w_paper1_placeholder→w_hand` alias), `write_cell_metrics` (per-cell `per_seed_metrics.csv` + `_meta.json` accumulating seeds for resume), `write_batch_summary`, `estimate_runtime_from_batch`, `run_batch` (wraps everything in F7 `FreezeInvariantContext`; smoke profile uses 50-host mini-fleet × 30 catalog CVEs × single t0 = 2024-09-01 cached). CLI `scripts/paper2_run_pilot_batch.py` refuses any non-`B-pilot-primary` batch and any primary batch (exit 2); refuses `--no-smoke` (Step-6 smoke only). Makefile: `paper2-pilot-B-pilot-primary` and `paper2-pilot-smoke-status`. Tests: **17 new** across `test_paper2_batch_runner.py` + `test_paper2_pilot_script.py` (refuses primary; refuses non-step6 batch; loads B-pilot-primary; K1/K3/S-A preflight trigger; calibration-cell preflight refusal; per-row freeze_witness_id tagging; resume-skips-completed; runtime summary fields). One trivial bug-fix to `scripts/paper2_run_pilot_batch.py` (path-insert so `paper2_runtime` imports under `make`). **Checks: full pytest 832 passed; ruff clean; compileall OK; `make verify-primary-freeze` OK before AND after.** **Real smoke run**: `make paper2-pilot-B-pilot-primary` → 12 cells × 6 seeds = **72 seed-runs in 22.4 s wallclock (mean 0.311 s / seed-run)**; freeze_witness_id = real manifest SHA-256 `750e144b…b022833`; 3 triggered rules (`K1`, `S-A`, `K3`) match Step-3.8; freeze_invariant_result.json status `OK` with all 5 assertions True (before/after OK, manifest SHA equal, no Paper-1 paths modified, no freeze-primary invocation); 361-line `per_seed_metrics.csv` (1 header + 360 rows = 12 × 6 × 5 metrics); every row carries `freeze_witness_id`. Paper 1 freeze verified OK after the smoke. **Paper 1 frozen artefact UNTOUCHED.** | **Step 7 ALLOWED** as a *full-pilot* step: (a) graduate the runner from smoke-light to the full F6 design (500 hosts × 18 t0s × all catalog CVEs); (b) launch the remaining 3 pilot batches (`B-pilot-capacity`, `B-pilot-blackout`, `B-pilot-ablation`); (c) compute `measured_per_seed_run_seconds` for the full pilot and project the primary runtime; (d) write `paper2/audit/pilot_gate_decision.json` (`PROCEED_TO_PRIMARY_NO_FALLBACK` if projection ≤ 18 h, else `APPLY_FALLBACK_ORDER_AND_REPLAN`); (e) NOT launch any primary batch until Step 8 confirms the pilot gate. Step 6 smoke produced a per-seed-run mean of 0.311 s but on a 50-host mini-fleet — the full pilot's per-seed-run cost will be substantially larger; Step 7 must re-measure honestly. |
| 7 — Full pilot runner + 4 pilot batches + pilot gate | 2026-05-28 | **Step 7 COMPLETE. Pilot gate = `PROCEED_TO_PRIMARY_NO_FALLBACK`.** | **Code**: extended `paper2_runtime/batch_runner.py` with the **full profile** (`profile="full"`, `--full --allow-step7-batch`): `_load_full_universe` (all NVD chunks, KEV, EPSS-2024-09-01 stand-in), `_catalog_match_full` (keyword scan against 17 keywords), `_make_fleet_full` (500 hosts), `_build_pair_features_full` (vectorised numpy outer product → 1 frame per (seed, t0), reused across cells), `_apply_ablation` (zero a feature + renormalise), `_score_full`, `_compute_cell_t0_metrics` (top-K via `np.argpartition`; per-blackout days), `_run_batch_full` (NVD universe + catalog match ONCE/batch; pair frames ONCE per (seed, t0); reused across cells; aggregates EHD across 18 t0s into one per-(cell, seed) row). Constants: `STEP7_ALLOWED_BATCHES = (B-pilot-{primary,capacity,blackout,ablation})`; `FULL_FLEET_SIZE=500`; `FULL_T0_LIST=(2023-09..2025-02, 18 monthly)`. **Pilot gate module**: new `paper2_runtime/pilot_gate.py` + `scripts/paper2_compute_pilot_gate.py` with `compute_pilot_gate()` (reads 4 batch summaries + 4 freeze results + 4 stop-rule evals; decision priority STOP > FALLBACK > PROCEED; safety factor 1.3). **Tests**: +12 new (6 full-profile + 6 pilot-gate); full pytest **844 passed**; ruff clean; compileall OK. Makefile: `paper2-pilot-B-pilot-primary-full`, `paper2-pilot-B-pilot-{capacity,blackout,ablation}`, `paper2-pilot-full`, `paper2-pilot-gate`. **Real full pilot run** (`make paper2-pilot-full`): all 4 batches completed; **48 cells; 288 seed-runs; total wallclock 459.54 s (7.66 min); measured per-seed-run = 1.596 s**; per batch breakdown — primary 12×6=72/114.3s/1.59 sr⁻¹, capacity 15×6=90/123.6s/1.37 sr⁻¹, blackout 9×6=54/87.5s/1.62 sr⁻¹, ablation 12×6=72/134.1s/1.86 sr⁻¹; **all 4 batches `freeze_status=OK`** (manifest SHA equal before/after; no Paper-1 paths modified; no `freeze-primary` invocation); every metric row carries the real witness ID `750e144b…b022833` (zero rows missing across 1,732 total rows). **Stop rules triggered: K1, K3, S-A** (Step-3.8 historical; same on every batch). **Projected primary runtime = 0.64 h** (1.596 s × 1,440 seed-runs ÷ 3,600); **with 1.3× safety factor = 0.83 h ≪ 18 h hard max**. No hard-halt rules; no fallback required. `make verify-primary-freeze` returned OK after the full pilot run. **Paper 1 frozen artefact UNTOUCHED** (verified 390 audit logs valid). | **Step 8 (primary launch) ALLOWED** as a **one-batch-at-a-time** step. Step 8 must: (a) launch exactly the 4 primary batches (`B-primary-{primary,capacity,blackout,ablation}`) via the same `--full` profile with a new `--allow-step8-batch` flag (or equivalent) to graduate from pilot-only enforcement; (b) use `planned_seeds=30` per F8; (c) wrap each batch in F7 freeze-invariant context; (d) write all per-seed metric rows with the freeze_witness_id; (e) after all 4 primary batches complete, write a `paper2/audit/primary_complete.json` and an updated `pilot_gate_decision`-style decision document; (f) NOT touch any Paper-1 frozen output. Step 9 (manuscript tables/figures + aggregation) and Step 10 (manuscript draft) remain blocked behind Step 8. |
| 8 — Four primary batches + primary completion gate | 2026-05-28 | **Step 8 COMPLETE. `PRIMARY_COMPLETE_VALID`.** All 4 primary batches valid; Paper 1 UNTOUCHED | **Code**: extended `paper2_runtime/batch_runner.py` with the **primary profile** (`profile="primary"`; `STEP8_ALLOWED_BATCHES=(B-primary-{primary,capacity,blackout,ablation})`; `PRIMARY_REQUIRED_SEEDS=30`; auto-detect from `B-primary-*` prefix; refuse any non-primary batch / pilot batch / `--max-seeds != 30` / missing `--allow-step8-batch`). New module **`paper2_runtime/primary_gate.py`** + `scripts/paper2_compute_primary_completion.py` with `compute_primary_completion()` (reads 4 batch summaries + 4 freeze results + 4 stop-rule evals + opens every `per_seed_metrics.csv` to validate every row carries `freeze_witness_id` AND no row uses a calibration / learned strategy; status priority `INVALID_FREEZE` > `INVALID_STOP_RULE` > `INCOMPLETE` > `INVALID_ROWS` > `VALID`). New CLI `scripts/paper2_run_primary_batch.py`. Makefile: `paper2-primary-B-primary-{primary,capacity,blackout,ablation}`, `paper2-primary-full`, `paper2-primary-status` (status-only; shell-only; never invokes runner), `paper2-primary-complete`. **Tests**: +19 new (`tests/test_paper2_primary.py`) covering runner refusals (pilot batch / non-primary / no allow-step8 / n != 30) AND gate decisions (VALID / INCOMPLETE / INVALID_FREEZE / INVALID_ROWS / INVALID_STOP_RULE / calibration-strategy rejection); full pytest **863 passed**; ruff clean; compileall OK. **Real primary execution** (background, harness-tracked, split across 2 launches after one tool-side kill mid-batch): all 4 batches completed via runner's resume semantics (`_completed_seeds` per `_meta.json`); **48 cells; 1,440 seed-runs; total wallclock 2,143.21 s (35.7 min); measured per-seed-run = 1.488 s** — per batch: primary 12×30=360 sr / 489.0 s / 1.358 sr⁻¹; capacity 15×30=450 sr / 604.8 s / 1.344 sr⁻¹; blackout 9×30=270 sr / 397.1 s / 1.471 sr⁻¹; ablation 12×30=360 sr / 652.4 s / 1.812 sr⁻¹. **All 4 batches `freeze_status=OK`** with all 5 F7 assertions True (before OK; after OK; manifest SHA byte-equal; no Paper-1 paths modified; no `freeze-primary` invocation). **Every metric row carries `freeze_witness_id`** = `750e144b…b022833` (0 / 8,640 rows missing — 2160 + 2700 + 1620 + 2160). **Stop rules triggered: K1, K3, S-A** (Step-3.8 historical; same on every batch). **No K5/K6 hard halts. No calibration/learned strategy rows. No primary forbidden operations.** `make verify-primary-freeze` returned OK after the full primary run (390 audit logs valid; freeze verification OK). **Paper 1 frozen artefact UNTOUCHED.** | **Step 9 ALLOWED** as a **manuscript-aggregation + tables/figures** step (still no paper drafting): (a) aggregate per-seed metric rows from `paper2/results/B-primary-*/per_seed_metrics.csv` into tables per F3 claim binding (CLM-B1 / CLM-B2 / CLM-C1..C4); (b) apply F4 inference policy (Wilcoxon + Holm + BCa CI gated by MDE; SM-1/3/4/5 enforcement; `kev_first` and oracle inference dropped); (c) evaluate K2 / K7 / K8 post-run rules over the primary metrics (post-run sensitivity ΔEHD per axis, K8 CV_within/CV_across, K2 all-axes-null check); (d) generate **only** tables / figures under `paper2/tables/` and `paper2/figures/` (NEW dirs; not `paper/`); (e) DO NOT draft the paper. Step 10 (manuscript draft) remains blocked behind Step 9. |
| 9 — Aggregation + inference + post-run stop rules + figures + audit | 2026-05-28 | **Step 9 COMPLETE.** All artefacts produced; `step10_manuscript_drafting_allowed = true` | **Code**: 4 new modules + 1 orchestrator. `paper2_runtime/aggregate.py` (loads 4 B-primary-* CSVs → 8,640 rows; validates row count + per-row freeze witness + K1 forbidden tokens + 30 seeds/cell; pivots to per-(cell, seed) wide frame; computes per-cell / per-strategy summaries; paired ΔEHD vs `epss_only` at central cell `P-epss_only-na`; axis sensitivity for capacity / blackout / weight family / feature ablation; scheduler-feasibility sentinel; refuses writes under `paper/` or `results/primary_full_v1/`). `paper2_runtime/inference.py` (uses Paper-1's `bootstrap_ci_bca`, `wilcoxon_signed_rank`, `holm_bonferroni`; B1/C1/C2/C3/C4 Holm families; SM-1 degeneracy auto-drop; SM-3 oracle disabled; SM-4 descriptive guardrail when Holm passes but BCa CI overlaps 0 or ⊂ ±5,000 hd; SM-5 diagnostic-only metric refusal; central-cell baseline; per-record `interpretation_guardrail` field). `paper2_runtime/post_run_stop_rules.py` (K2 = |median ΔEHD| < 1,000 hd AND CI ⊂ ±5,000 hd per axis; K7 SKIPPED with rationale — no perturbation data; K8 CV_within / CV_across per axis). `paper2_runtime/figures.py` (matplotlib only — **NO seaborn**; Agg backend; 7 figures × PNG + PDF = 14 files: ΔEHD vs epss_only bar, weight-family boxplot, capacity-sensitivity lines, blackout-sensitivity lines, feature-ablation bars per vector, weight-family Spearman heatmap, scheduler-feasibility sentinel with K4 floor). `paper2_runtime/step9_audit.py` (builds + writes the audit artefact with `step10_manuscript_drafting_allowed` flag and the full file manifest). Orchestrator `scripts/paper2_generate_step9_outputs.py`; Makefile target `paper2-step9`. **Tests**: +17 new (`tests/test_paper2_step9.py`) covering aggregation refusals + delta-vs-epss baseline + SM-1 degeneracy + SM-4 descriptive guardrail + K7 SKIPPED + K2/K8 evaluation + no-seaborn-import + figures non-empty + Paper-1 path refusal + audit artefact; full pytest **880 passed**; ruff clean; compileall OK. **Real Step-9 run**: loaded 8,640 metric rows → 1,440 per-(cell, seed); wrote **19 aggregation files + 12 inference files + 3 post-run files + 14 figures + 2 audit files = 50 total** under `paper2/tables/`, `paper2/tables/inference/`, `paper2/figures/`, `paper2/audit/` (zero writes under Paper-1 territory). **`step9_aggregation_complete.json`**: `primary_complete_status = PRIMARY_COMPLETE_VALID`; `freeze_verification_after_step9 = OK`; `paper1_freeze_manifest_sha256 = 750e144b…b022833`; warnings = `[]`; `step10_manuscript_drafting_allowed = true`. Post-run stop rules evaluated: **K2, K7 (SKIPPED), K8** — see `paper2/audit/post_run_stop_rule_evaluation.json` for per-axis values. `make verify-primary-freeze` returned OK after Step 9 (390 audit logs valid). **Paper 1 frozen artefact UNTOUCHED.** | **Step 10 ALLOWED** as a **manuscript-drafting** step: (a) draft `paper2/manuscript/paper2_full_draft.md` using ONLY the Step-9 audit-listed tables and figures + the F1-mandated framing changes (drop "Robustness…" headline; elevate failure-aware-gate + negative result to PRIMARY contribution; demote sensitivity sweeps to CONFIRMATORY; acknowledge synthetic fleet as Limitations; cite P1–P15 with visible differentiation; drop "fixed/published weights"; no `[CITATION]` placeholders); (b) use the Step-3.18 §3 chosen title (Title #1: *"When Calibration Fails: A Failure-Aware Public-Feed Gate for Vulnerability Prioritization Under Sparse Exploit Labels"*); (c) implement the F3 §6 + F5 SM-4 / SM-5 forbidden-phrase guard (CI scan) over the draft; (d) NOT generate new tables / figures (those are frozen at Step 9); (e) NOT modify Paper-1 frozen outputs; (f) include the K7 SKIPPED rationale as a Limitations item. Step 11 (final claim + citation audit + submission packaging) remains blocked behind Step 10. |
| 10 — Manuscript draft + claim-audit script | 2026-05-28 | **Step 10 COMPLETE.** Draft written; claim audit `PASS` on 0 violations; Paper 1 UNTOUCHED | **New manuscript** `paper2/manuscript/paper2_full_draft.md` (17 sections): title #1 from F9; abstract surfacing 7 unique positives / 2,688 catalog CVEs / 18 monthly t0s / calibration not attempted / failure-aware-gate as primary contribution / sensitivity as confirmatory; Introduction explaining why public-feed calibration is fragile under sparse labels (CVE-not-record-as-calibration-unit, leakage-safe Label A); Background restating the Paper-1 linear scoring + EHD; Related Work positioning against P1–P15 with explicit one-sentence differentiation each (VULCON / Deep VULMAN / Roytman / Sherif 2026 / Ravalico 2025 / Koscinski 2025 / Jacobs et al. / Allodi & Massacci / SSVC / NIST LEV / VulRG / VulnScore / Jiang survey / VM-Chaining); Problem Statement bound to Q1/Q2/Q3 mapped to CLM-A1 / CLM-C* / CLM-E1/F1; Failure-Aware Gate methodology (acquisition + multi-t0 + stop rules + audit); Study Design referencing F2–F9; Public-Feed Feasibility (110,224 NVD recs, 11 chunks, 18 t0s, 7 unique positives, K1/K3/S-A triggered); Fixed-Prior Sensitivity Design (48 cells, 30 seeds, deferred items listed, K1-forbidden cells listed); Metrics + Inference Policy (EHD primary, BCa CI, SM-1/3/4/5, diagnostic-only D1/G1); Results 11.1–11.9 with REAL numbers from Step-9 tables (baseline EHD ≈ 3.32×10⁹ host-days; 11 B1 inference records all "allowed/descriptive"; 15 C1 pairwise; 5 C2; 3 C3; ablation per vector; scheduler_feasibility=1.0 everywhere; K2 does NOT fire / K7 SKIPPED / K8 fires on every axis → each axis descriptive-only); Discussion explaining sparse-label methodological constraint; Limitations including synthetic fleet, 31-product catalog, CPE matching, KEV-only Label A, K7 SKIPPED, no PoC, no EPSS v4, fixed priors, no calibration, sparse positives, no production, no government, no compliance, K8 demotion, helper return-path note; Future Work; Conclusion; Reproducibility + Artifact Notes; References (P1–P15 with `[VERIFY]` markers preserved). **No `[CITATION]` placeholders.** Figures referenced by relative path only — not embedded. **New script** `scripts/paper2_claim_audit.py` with always-forbidden phrases (outperforms / superior / state-of-the-art / government deployment / compliance achieved / calibrated model improves / learned model / "first ever"), negatable-with-negation (validated / production), 100-char proximity check for "significant" near {precision_at_k / recall_at_k / nDCG / KEV breach / diagnostic}, 200-char proximity check for "pair count" near "sample size" / "effective N", positive-calibration-claim detector (with 80-char negation window), and context-aware-priors-beat-EPSS regex. **Tests** `tests/test_paper2_claim_audit.py` (25 tests): always-forbidden / negation handling / proximity rules / pass-on-safe-draft / fail-on-unsafe / CLI exit codes. **Checks**: full pytest **905 passed**; ruff clean; compileall OK; `make verify-primary-freeze` OK after Step 10. **Claim audit on the draft: `PASS  paper2/manuscript/paper2_full_draft.md (0 violations)`.** During drafting the audit caught 5 violations (the literal word "superiority"; the phrase "learned model" even when prefixed with "no"; three "pair count … sample size / effective N" proximity matches); each was rephrased without changing semantics (e.g., "the host-CVE record count is not the calibration unit"); the final draft is clean. **Paper 1 frozen artefact UNTOUCHED.** | **Step 11 ALLOWED** as a **final claim-and-citation audit + submission-packaging** step: (a) re-verify the `[VERIFY]` items left in references (VULCON DOI 10.1145/3196884; NIST CSWP 41 LEV; VulnScore Springer IJIS 2025) — replace any that cannot be verified with the next-best verified citation; (b) compile the draft into a venue-target format (USENIX CSET LaTeX template per F9; LASER / DTRAP as backups); (c) build the reproducibility appendix mapping every numeric claim to a Step-9 source file; (d) run the claim audit + ruff + pytest + freeze-verify a final time; (e) NOT modify Paper-1 frozen outputs; (f) NOT add new tables / figures. Step 11 produces a submission-ready bundle but **does not submit**. |
| 11 — Final claim/citation audit + submission packaging | 2026-05-28 | **Step 11 COMPLETE.** Submission bundle assembled; claim + citation audits PASS; freeze OK; **NOT submitted** | **Three `[VERIFY]` blockers resolved**: VULCON re-verified via Crossref `10.1145/3196884` (author "Sushil" was given-name; family name **Jajodia**; corrected throughout); NIST CSWP 41 attributed to **Mell + Spring** (May 2025) via NIST press release / official PDF; VulnScore re-verified via Crossref `10.1007/s10207-025-01164-3` (full title "VulnScore: A deployed system for patch prioritization combining human input and temporal threat intelligence"). All `[VERIFY]` markers removed from `paper2_full_draft.md`. **New submission tree** `paper2/submission/`: `README.md` (package contents, target venue CSET, backups LASER + DTRAP, what-not-to-claim, remaining blockers), `REPRODUCIBILITY_APPENDIX.md` (26-row table mapping each numeric claim to source file + field — 7 unique positives / 2,688 catalog / 18 t0s / 4+4 batches / 288 pilot + 1,440 primary seed-runs / 8,640 metric rows / K1/K3/S-A / PRIMARY_COMPLETE_VALID / Step-9 counts / freeze SHA / K2 not-fire / K7 SKIPPED / K8 fires-every-axis / no-learned-cells), `CLAIM_AUDIT_REPORT.md` (audit invocations + PASS verdict + waivers-none), `CITATION_AUDIT_REPORT.md` (15 verified entries + 4 items flagged for routine human review + 0 unresolved [VERIFY] + risk assessment). **LaTeX scaffold** `paper2/submission/cset/`: `main.tex` (neutral `\documentclass{article}` with `[VERIFY with current CSET CFP/template]` marker; 16 section inputs + `\bibliography{references}`), `references.bib` (15 verified BibTeX entries), `sections/00_abstract.tex`..`sections/15_reproducibility.tex` (16 files auto-converted via new `scripts/paper2_md_to_latex.py` — split-on-H2; conservative inline markdown→LaTeX; wording preserved verbatim), `figures/` (14 = 7 PNG + 7 PDF, copied from `paper2/figures/`), `tables/` (33 = 19 top-level + 12 inference + 1 wide; copied from `paper2/tables/`), `cset/README.md` (template + compile + formatting TODOs). **New script** `scripts/paper2_md_to_latex.py`. **Final checks**: `python -m compileall -q scripts/ paper2_runtime/` OK; `ruff check` PASS ("All checks passed!"); `pytest tests/test_paper2_*.py` **154 passed in 26.71 s**; `claim_audit paper2_full_draft.md` PASS 0 violations; `claim_audit /tmp/paper2_concat.tex` (concatenated LaTeX sections) PASS 0 violations; Paper 1 freeze SHA `750e144b…b022833` byte-equal in `paper/report/report_manifest.json` AND carried as `freeze_witness_id` on all 4 primary batch summaries. **No submission performed**; **no Paper-1 modification**; **no new tables / figures generated**. **Standing rules honored**: did NOT submit; did NOT change F2–F9 / Step-4 pre-registration; did NOT weaken the claim audit; did NOT fake / fabricate / introduce claims; did NOT add EB1A/USCIS language; PoC stays license-gated; NVD_API_KEY value never printed. **Closeout** at `paper2/manuscript/STEP11_CLOSEOUT.md`. | **No further automated steps.** The bundle is held for human reviewer to (a) swap the venue-specific document class into `main.tex`; (b) replace the placeholder author block; (c) resolve the 4 routine pre-submission citation chores (two `{...authors}` placeholders, arXiv ID re-check on Sherif2026KRI, prefer venue version of Ravalico2025 if available); (d) run `pdflatex main.tex; bibtex main; pdflatex main.tex; pdflatex main.tex`; (e) submit to USENIX CSET (or LASER / DTRAP as backups). None of these operations were performed in Step 11. |

## Current status
**Pivot held but reframed: CONDITIONAL KEEP** (Step 3.10 / F1).

The Step-3.9 robustness/sensitivity headline collides with substantial prior work
(VULCON 2018, Deep VULMAN 2023, Roytman "Capacity is King", Sherif et al. 2026
KRI ablation, Ravalico et al. 2025 EPSS temporal dynamics, Koscinski et al. 2025
4-system disagreement, Jiang et al. 2025 survey) — sensitivity / ablation /
capacity-constrained prioritization are NOT novel as a primary contribution.
**Surviving novel piece** (per F1 falsification): the failure-aware multi-t0
calibration-gate methodology + the specific negative result that per-feature
weight calibration on KEV-only labels intersected with a public-sector-typical
31-product catalog over the EPSS v3 era is infeasible (7 unique positive CVEs
over 18 monthly t0 windows; Step 3.8). Sensitivity sweeps remain as
*confirmatory secondary* tables only, never as the headline claim.

Honest scoring (post-F1): publishability 4/10, novelty 3/10, EB1A value 3/10.
Decision rule still applies: pivot survives only with the seven framing changes
in `STEP3_10_PRIOR_ART_F1.md §9` baked into the eventual pre-registration.

Decision: **CONDITIONAL KEEP** to Step 4. **F1–F9 all COMPLETE.**
**Step 4 pre-registration: COMPLETE.** **Step 5 implementation: COMPLETE.**
The design is now immutable; the runtime package is built and tested.

`paper2_runtime/` is now a real, tested, importable Python package with 53
new tests (full suite 815 passed; ruff clean; compile OK; Paper-1 freeze
verified OK before and after Step-5 work). The package contains: the F5
stop-rule registry loader + static evaluator (K1 / K3 / S-A correctly fire
for the Step-3.8 measured `unique_positive_distinct_cves = 7`); the F7
freeze-invariant wrapper with the 4-witness audit schema + context-manager
violation path; the F6 cell loader (66 rows = 48 planned + 10 reused + 8
deferred) and F8 batch loader (4 pilot + 4 primary = 1,728 seed-runs); the
F2 weight registrar (registers 4 new vectors; Paper-1 built-ins assertion-
locked); the F4 inference-policy shim (SM-1 degeneracy, SM-3 oracle-disabled,
SM-5 diagnostic-no-significance); the read-only F6+F8 run planner.

**Step 6 implementation + smoke: COMPLETE.** Real one-batch smoke
(`B-pilot-primary`, 12 cells × 6 seeds = 72 seed-runs) ran in **22.4 s
wallclock** on real cached public-feed data with the freeze invariant held
(witness ID = real manifest SHA-256 `750e144b…b022833`); 3 rules (`K1`,
`S-A`, `K3`) triggered as the Step-3.8 measurement requires; 360 metric
rows written, each tagged with the freeze witness id; freeze verified OK
before AND after; Paper-1 frozen artefact UNTOUCHED.

**Step 7 ALLOWED** as a **full-pilot** step: graduate the runner to the
full F6 design (500 hosts × 18 t0s × all catalog CVEs); launch the
remaining 3 pilot batches (`B-pilot-capacity`, `B-pilot-blackout`,
`B-pilot-ablation`); compute `measured_per_seed_run_seconds` for the full
pilot; write `paper2/audit/pilot_gate_decision.json`
(`PROCEED_TO_PRIMARY_NO_FALLBACK` if projected primary ≤ 18 h, else
`APPLY_FALLBACK_ORDER_AND_REPLAN`). Step 8 (primary launch, only on
`PROCEED_TO_PRIMARY_NO_FALLBACK`) remains blocked behind Step 7.
**No Paper-1 modification.**

Resolved so far:
- **F2** (Step 3.11): the prioritization weight vector is itself a
  pre-registered sensitivity axis over six fixed design-prior vectors
  (`w_uniform`, `w_paper1_placeholder`, `w_epss_dominant`, `w_cvss_dominant`,
  `w_kev_dominant`, `w_context_dominant`); see `STEP3_11_FIX_F2_WEIGHTS.md`.
- **F3** (Step 3.12): metric → claim binding locked for 13 claims
  (CLM-A1, CLM-A2, CLM-B1..3, CLM-C1..4, CLM-D1 diag, CLM-E1..2, CLM-F1..2,
  CLM-G1 diag); axis → claim mapping locked; 13 forbidden claims and 13 stop
  rules pre-registered; see `STEP3_12_FIX_F3_METRIC_CLAIM_BINDING.md`. Class A
  primary metric = **unique positive distinct CVEs**; Class B primary metric =
  **EHD**; Class C primary = **sensitivity ΔEHD across A3 / A1 / A6 / A5**;
  ranking@k and KEV breach rate are **diagnostic-only** under Step-3.8 sparsity.
- **F4** (Step 3.13): MDE / power pre-registered using real variance prior from
  Paper 1's frozen 30-seed EHD distribution (baseline `epss_only` = 1,121,903
  host-days; paired-Δ std 2,331–3,673 hd; `kev_first` degenerate). **Meaningful
  threshold = 5,000 hd (≈0.45 %)** (chosen above noise floor; suggested 1,000 hd
  / 0.10 % was below noise and would collapse the gate). At n = 30 with ×1.5
  sparsity inflation, MDE-hd 1,234–2,916 < meaningful 5,000 → **inference
  allowed for CLM-B1 (except `kev_first`), CLM-B2, CLM-C1..C4**;
  **descriptive-only for CLM-B3** (sparse oracle); **diagnostic-only for
  CLM-D1, CLM-G1**; sentinel / binary / measurement for the rest. Wilcoxon +
  Holm gated by SM-1..SM-6 stop rules; see
  `STEP3_13_FIX_F4_MDE_POWER.md`.
- **F5** (Step 3.14): canonical stop-rule registry merges Step-3.14 K1–K6,
  Step-3.9 K1–K6 (preserved via canonical IDs K2/K7/K8/K5.a), F3 S-A..S-G1,
  and F4 SM-1..SM-6 into one machine-readable YAML schema with computable
  triggers, locked thresholds, and explicit actions. Categories: acquisition /
  calibration / metric-validity / statistical-inference / scheduler / audit-freeze
  / manuscript-claim. **Currently triggered: K1 (calibration infeasibility,
  measured 7 < 20), K3 (label sparsity), S-A (mirrors K1)**; all others
  NOT_MEASURED until Step 4 produces a per-seed metric frame. Step-4
  implementation contract: load YAML → pre-flight gate → per-cell gate →
  policy-shimmed inference → post-run gate → manuscript-CI gate →
  audit-completeness self-policing. See `STEP3_14_FIX_F5_KILL_CRITERIA.md`.
- **F6** (Step 3.15): minimal factorial cells locked at
  `paper2/design/STEP3_15_MINIMAL_FACTORIAL_CELLS.{csv,yaml}`. **48 new
  runnable cells + 10 reused + 8 deferred = 1,440 planned seed-runs at n=30**:
  primary (12) for CLM-B1/B2/B3/C1, capacity sensitivity (15+5) for CLM-C2,
  blackout sensitivity (9+3) for CLM-C3, feature ablation (12+2) for CLM-C4.
  No calibrated / learned / GBT cells (K1 forbids). All non-PoC defaults
  honored. Runtime estimate **8–24 h on a laptop** with shared per-seed
  computation; six fallback options (F8-a..F8-f) pre-listed for F8 to choose
  from. See `STEP3_15_FIX_F6_MINIMAL_FACTORIAL.md`.
- **F9** (Step 3.18): venue plan + change-our-minds clause locked. Final
  framing committed (Step-3.10 §9 items 1–7); title #1 recommended:
  *"When Calibration Fails: A Failure-Aware Public-Feed Gate for Vulnerability
  Prioritization Under Sparse Exploit Labels"*. Venue strategy: primary
  **USENIX CSET**, backup 1 **LASER**, backup 2 **ACM DTRAP**; top-tier
  venues honestly rejected as poor fit. Change-our-minds clause encodes
  **6 KILL** (no venue home; pilot infeasible even after F8-a; K2 + reviewer
  trivial; total degeneracy; no insight; later-discovered prior art),
  **4 PIVOT** (all Δs zero; catalog expansion needed; EPSS-only dominates;
  acquisition-only), and **5 KEEP** conditions. Step-4 entry criteria: 11
  items locked including K1+K3-triggered-with-measured-7 in pre-registration
  abstract. Carried `[VERIFY]` (4 items: VULCON DOI, NIST CSWP 41 LEV,
  VulnScore Springer IJIS, CSET-2026 / LASER-2026 CFP URLs) re-attempted —
  no fabrication; all marked for human re-verification at submission time.
  See `STEP3_18_FIX_F9_VENUE_PLAN.md`.
- **F8** (Step 3.17): compute envelope locked (target ≤12 h, hard max 18 h,
  `max_workers=4` seed-level parallel, ≤16 GB / ≤25 GB). Chosen plan = **F8-f
  pilot-then-primary**: pilot 4 batches × 6 seeds = 288 seed-runs (est. 2–4 h);
  primary 4 batches × 30 seeds = 1,440 seed-runs (est. 9.8–19.5 h serial /
  3–6 h with 4 workers) — gated on pilot projection ≤ 18 h. Deterministic
  fallback order F8-e → F8-d → F8-c → F8-b → F8-a (last resort) fires only if
  projection exceeds the ceiling; `pilot_only_fallback` closed by default.
  Shared-computation architecture binding: NVD universe ONCE/run; EPSS/KEV
  as-of maps ONCE/t0; fleet ONCE/seed; pair frames and feature frames ONCE/
  (seed, t0), reused across all strategies / weight vectors / capacities /
  blackouts / approvers / ablations. F7 freeze wrap on EVERY batch.
  Abort / resume: ×1.5 budget overrun pauses + manual approval; resume skips
  completed checkpoints unless `--force`. Companion file
  `paper2/design/STEP3_17_PLANNED_BATCHES.yaml` does NOT mutate the F6
  source-of-truth CSV/YAML. See `STEP3_17_FIX_F8_COMPUTE_BUDGET.md`.
- **F7** (Step 3.16): Paper-1 freeze-invariant contract locked. Reference
  manifest `results/primary_full_v1/FREEZE_MANIFEST.json` SHA-256
  **`750e144b…b022833`**. Every Paper-2 batch wraps with `make
  verify-primary-freeze` + manifest SHA + `git status --porcelain` before
  and after; 5 hard assertions on the post-run gate; K6 HARD HALT on any
  failure. Allowed writes: `paper2/`, `results/paper2/`, `data/cache/`,
  `data/snapshots/`. Forbidden writes: `results/primary_full_v1/`,
  `paper/{tables,figures,manuscript,acm,eb1a}/`, `FREEZE_MANIFEST.json`.
  Forbidden ops: `freeze-primary`, `inspect-primary --freeze`,
  `register_calibrated_weights` (K1). Per-batch witness ID propagated to
  every cell row; 4 audit files per batch + append-only batches index. CI
  gate fails manuscript regen on 8 well-defined conditions. Paper-1
  weight-registry lock test spec'd (assert exact `w_uniform`/`w_hand`/
  `w_logit_placeholder`/`w_lin_placeholder` keys + values + sum-to-1 +
  R-as-cost + reject overwrites + F2 vector registrations leave built-ins
  unchanged). Step-4 implementation lives under new `paper2_runtime/`
  package (NOT `src/paper1/`). See `STEP3_16_FIX_F7_FREEZE_INVARIANT.md`.

Paper 1's frozen benchmark remains untouched and read-only throughout. Catalog
expansion still deferred; no calibration claim; no superiority claim.

## Decision rules (Step 3 gate)
- ≥50 positive distinct CVEs + non-degenerate calibrated weights → **GO** to
  pre-registration.
- 20–49 positives → **CONDITIONAL GO**: expand date range / product catalog.
- <20 positives → **PIVOT** away from calibration (robustness/sensitivity only).
- Public-feed fetch blocked (e.g., no network) → **CONDITIONAL GO pending data
  acquisition** (do not fake success).
- CPE mapping yields too few CVEs → expand catalog or pivot.

## Invariants (all steps)
Paper 1 frozen outputs (`results/primary_full_v1/`) are never modified. Probe writes
only under `paper2/feasibility/`, `data/snapshots/`, `data/cache/`. PoC/ExploitDB
disabled unless the license gate is explicitly set; PoC data never redistributed.
No fabricated data or citations.
