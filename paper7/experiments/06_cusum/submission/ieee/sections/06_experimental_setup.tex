\section{Experimental Setup}

\subsection{Pre-Registration}

CUSUM parameters $k = 0.04$, $h = 0.10$, strategy set, hypotheses
H1--H4, decision rules, and stop rules were locked in
\texttt{paper13/design/PAPER13\_PROTOCOL.md} on 2026-06-05 before
any CUSUM-detector P@50 was inspected.

\subsection{Execution}

For each $K$: cache states, per window fit lag-1 / offline weights
and compute $\Delta_w$, update the CUSUM accumulator and the
cap\_aware fire decision, score all 25 evaluation seeds with each
of six strategies' weights.

\subsection{Reporting}

Cell-mean point estimates over 25 evaluation seeds. 95\% percentile
bootstrap CIs (10{,}000 resamples). H3 is a strict-inequality
comparison on fire fractions (single value per K).

\subsection{Reproducibility}

{\small\begin{verbatim}
PYTHONPATH=src python3 src/run_cusum.py
PYTHONPATH=src python3 src/analyze.py
PYTHONPATH=src python3 src/make_figures.py
\end{verbatim}}
